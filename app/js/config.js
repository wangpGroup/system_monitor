/**
 * Configuration
 */

var Config = {
    debug: false,
    devtools: false,
    silent: true,
    
    realtimeLen: 60,
    realtimeIntervalTime: 1000, // 实时刷新间隔时间（ms）
    hourInterval: 1, // 一小时区间中每几分钟一个点
    dayInterval: 5   // 一天区间中每几分钟一个点
    
};
